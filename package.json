{
  "name": "semantic-template",
  "version": "1.7.0",
  "description": "A Template repository for semantically versioned projects",
  "author": "PatEvs",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/patevs/semantic-template.git"
  },
  "engines": {
    "node": ">=10.21.0",
    "npm": ">=6.8.0"
  },
  "scripts": {
    "prepare": "husky install",
    "test": "echo \"Error: no test specified\" && exit 0",
    "updates": "npx ncu",
    "release": "npx np"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.2.0",
    "commitlint-config-gitmoji": "^2.2.6",
    "husky": "^8.0.1"
  }
}
